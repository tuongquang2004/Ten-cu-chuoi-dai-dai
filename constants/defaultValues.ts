import { FormProps } from "./types"

export const defaultForm: FormProps = {
    label: "Form",
    buttonLabel: "Submit",
    action: ""
}

export const defaultRefSrc = {
    id: "",
    isActive: true,
    source: "",
}